import { HeaderBoldDirective } from './header-bold.directive';

describe('HeaderBoldDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderBoldDirective();
    expect(directive).toBeTruthy();
  });
});
