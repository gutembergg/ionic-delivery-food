import { ShowTitleDirective } from './show-title.directive';

describe('ShowTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowTitleDirective();
    expect(directive).toBeTruthy();
  });
});
