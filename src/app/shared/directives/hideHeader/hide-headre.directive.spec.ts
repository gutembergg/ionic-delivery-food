import { HideHeadreDirective } from './hide-headre.directive';

describe('HideHeadreDirective', () => {
  it('should create an instance', () => {
    const directive = new HideHeadreDirective();
    expect(directive).toBeTruthy();
  });
});
